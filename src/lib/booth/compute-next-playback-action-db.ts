export async function computeNextPlaybackActionDb(_locationId: string) {
  throw new Error(
    "computeNextPlaybackActionDb is not wired yet. Keep using the existing compute-next-playback-action flow for now."
  );
}